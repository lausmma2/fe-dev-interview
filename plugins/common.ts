// import { OpenAPI } from '~/apiClient';

export default defineNuxtPlugin((nuxtApp) => {
  // OpenAPI.BASE = nuxtApp.$config.public.apiBasePath ?? OpenAPI.BASE;
  // OpenAPI.TOKEN = ... - once logged in, set bearer auth token for secured endpoints;
  return;
  console.log(nuxtApp);
});
